This directory contains static files for the IBAL Chat XBlock.

The directory structure is:
  css/ - CSS files
  js/ - JavaScript files
  html/ - HTML templates

These files are served by the XBlock runtime when the XBlock is rendered. 